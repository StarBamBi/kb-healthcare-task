export interface Task {
  id: string;
  title: string;
  memo: string;
  status: "TODO" | "DONE";
}

export interface Task {
  id: string;
  title: string;
  memo: string;
  status: "TODO" | "DONE";
}

export interface TaskDetail {
  title: string;
  memo: string;
  registerDatetime: string;
}
